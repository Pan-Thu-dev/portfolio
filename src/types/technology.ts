export type Technology = {
  id?: string; // Firestore Document ID
  name: string;
  // Optional: Add fields like iconUrl, category later
  createdAt?: string; // ISO date string
  updatedAt?: string; // ISO date string
}; 